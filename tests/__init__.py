"""Unit tests for this repository's validation scripts.

This file exists so that `python -m unittest discover`, run from the
repository root with no arguments, recurses into `tests/` — plain
directories are skipped by discovery, packages are not.
"""
